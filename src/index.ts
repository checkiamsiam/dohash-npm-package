import checkHash from "./features/checkHash";
import createHash from "./features/createHash";
import decodeHash from "./features/decodeHash";

const doHash = { createHash, decodeHash, checkHash };

export { createHash, decodeHash, checkHash };

export default doHash;
